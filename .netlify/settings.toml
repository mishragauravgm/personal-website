# Netlify settings
# Disable auto-detection and force use of netlify.toml configuration

[build]
  ignore = false

[dev]
  framework = "hugo"
  command = "hugo server -D"
  port = 1313
  publish = "public"
